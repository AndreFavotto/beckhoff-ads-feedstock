--- meson.build	2026-06-09 13:40:20.959676911 +0200
+++ meson.build	2026-06-09 13:40:21.046021673 +0200
@@ -66,17 +66,29 @@
   'AdsLib/Semaphore.h',
   'AdsLib/Sockets.h',
   'AdsLib/SymbolAccess.h',
-  'AdsLib/TwinCAT/AdsDef.h',
-  'AdsLib/TwinCAT/AdsLib.h',
+  'AdsLib/wrap_endian.h',
+  'AdsLib/wrap_registry.h',
+  'AdsLib/wrap_socket.h',
+  subdir: 'AdsLib'
+)
+
+install_headers(
   'AdsLib/bhf/ParameterList.h',
   'AdsLib/bhf/StringToInteger.h',
   'AdsLib/bhf/WindowsQuirks.h',
+  subdir: 'AdsLib/bhf'
+)
+
+install_headers(
   'AdsLib/standalone/AdsDef.h',
   'AdsLib/standalone/AdsLib.h',
-  'AdsLib/wrap_endian.h',
-  'AdsLib/wrap_registry.h',
-  'AdsLib/wrap_socket.h',
-  subdir: 'AdsLib'
+  subdir: 'AdsLib/standalone'
+)
+
+install_headers(
+  'AdsLib/TwinCAT/AdsDef.h',
+  'AdsLib/TwinCAT/AdsLib.h',
+  subdir: 'AdsLib/TwinCAT'
 )
 
 inc = include_directories([
@@ -89,13 +101,20 @@
   dependency('threads'),
 ]
 
-adslib = static_library('AdsLib',
+adslib_static = static_library('AdsLib',
   [common_files, router_files],
   include_directories: inc,
   install: true,
 )
 
-install_libs = [ adslib ]
+adslib = shared_library('AdsLib',
+  [common_files, router_files],
+  include_directories: inc,
+  dependencies: libs,
+  install: true,
+)
+
+install_libs = [ adslib_static, adslib ]
 
 adslib_dep = declare_dependency(
   include_directories : inc,
@@ -133,6 +152,8 @@
   include_directories: inc,
   dependencies: libs,
   link_with: adslib,
+  install: true,
+  install_dir: get_option('bindir'),
 )
 
 if get_option('tcadsdll_include') != ''
@@ -163,6 +184,8 @@
       tcadsdll_deps
     ],
     link_with: tcadslib,
+    install: true,
+    install_dir: get_option('bindir'),
   )
 
   tcadstest = executable('tcAdsTest',
